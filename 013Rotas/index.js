/**
 * Nos sites e nas APIs temos oque chamamos de rotas
 * 
 * Que é basicamente uma URL que queremos acessar
 * 
 * Porém quando tratamos de uma API chamamos URL de rota
 * 
 * No express criamos nossas rotas para serem acessadas
 * via HTTP e responderem baseadas na lógica que queremos
 * 
 * Cada rota vai receber um método HTTP, que deve coincidir
 * com o proposto pelo REST
 */